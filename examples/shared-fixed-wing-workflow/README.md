# Shared fixed-wing workflow

Runs the fixed-wing model through `model.vehicle`, `trim.vehicle`,
`linearize.shared`, `sim.vehicle`, and the shared trajectory reports.

```sh
galata run examples/shared-fixed-wing-workflow/study.yaml
```

This is shared-architecture evidence, not a replacement for the historical
NT-33A study or aircraft validation.
